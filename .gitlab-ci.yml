variables:
  DOMAIN: "www.google.com"

stages:
 - build

build:
  before_script:
    - echo Hello World
  script:
    - "curl -X POST -F token=GITLAB_CI_TOKEN -F ref=GITLAB_CI_REFNAME https://gitlab.com/api/v4/projects/18680622/trigger/pipeline"
    - nslookup $DOMAIN